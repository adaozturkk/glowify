@model int

<a asp-area="Customer" asp-controller="Cart" asp-action="Index" class="nav-link text-dark fw-medium d-flex align-items-center gap-1">
    <i class="bi bi-cart"></i>
    <span>Cart</span>

    @if (Model > 0)
    {
        <span>(@Model)</span>
    }
</a>